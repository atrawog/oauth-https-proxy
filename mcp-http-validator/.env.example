# MCP HTTP Validator Configuration Example
# Copy this file to .env and update with your values

# Manual OAuth Access Token (optional - for pre-authenticated testing)
# MCP_ACCESS_TOKEN=your-mcp-access-token-here

# OAuth Client Credentials (automatically populated by the validator)
# Format: OAUTH_CLIENT_ID_{SERVER_KEY}
# The validator will automatically:
# 1. Discover OAuth servers from MCP server metadata
# 2. Register OAuth clients if needed
# 3. Save credentials in this format:
#
# OAUTH_CLIENT_ID_MCP_EXAMPLE_COM=mcp_client_123456
# OAUTH_CLIENT_SECRET_MCP_EXAMPLE_COM=secret_abcdef...
# OAUTH_REGISTRATION_TOKEN_MCP_EXAMPLE_COM=reg_token_xyz...
#
# Default credentials (used if server-specific not found):
# OAUTH_CLIENT_ID=default-client-id
# OAUTH_CLIENT_SECRET=default-client-secret

# Optional: SSL Verification (set to false for self-signed certs)
# SSL_VERIFY=true

# Optional: Request Timeout (seconds)
# REQUEST_TIMEOUT=30

# Optional: Output Directory for Reports
# REPORT_OUTPUT_DIR=./reports